import clsx from "clsx";

export default function HomePage() {
  return (
    <div>
      <h1
        className={clsx(
          "text-xl, font-bold, text-blue, transition duration-300",
        )}
      >
        texto de teste
      </h1>
    </div>
  );
}
